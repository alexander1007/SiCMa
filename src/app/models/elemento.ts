/**
 * Modelo que representa a un elemento del sistema. 
 * @author Alexander
 * @version 1.0
 */
export interface Elemento {
   key?: string;
    descripcion: string;
    fondo: string;
    nombre: string;
}